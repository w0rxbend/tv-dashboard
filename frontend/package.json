{
  "name": "tv-dashboard",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "vite build",
    "lint": "node scripts/check-source-standards.mjs",
    "format:check": "git diff --check -- .",
    "test": "node --conditions browser --import ../backend/node_modules/tsx/dist/esm/index.mjs --test src/**/*.test.js",
    "check": "npm run format:check && npm run lint && npm run test && npm run build",
    "preview": "vite preview"
  },
  "dependencies": {
    "gsap": "^3.15.0",
    "solid-js": "^1.9.3"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vite-plugin-solid": "^2.10.2"
  }
}
